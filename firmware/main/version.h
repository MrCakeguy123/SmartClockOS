#pragma once

// Default SmartClock OS version string. Can be overridden at build time by defining
// SMARTCLOCK_OS_VERSION via compiler flags.
#ifndef SMARTCLOCK_OS_VERSION
#define SMARTCLOCK_OS_VERSION "v0.1.0"
#endif

